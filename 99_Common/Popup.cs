using UnityEngine;

namespace SuperVeigar
{
    public class Popup : MonoBehaviour
    {
        protected readonly Color COLOR_UNSELECTED = new Color(0.3f, 0.3f, 0.3f);
        protected readonly Color COLOR_SELECTED = new Color(1f, 1f, 1f);
    }
}

